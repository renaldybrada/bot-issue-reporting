active: true

fallback: true

intents:
    fallbackIntent:
        type: text
        fallback: true

states:
    init:
        initial: true
        action: showDontUnderstand
        end: true
        transitions:
            init:
                fallback: true

actions:
    showDontUnderstand:
        type: text
        options:
            text: 'Maaf, saya belum mengerti maksud anda'
            
methods: {}
